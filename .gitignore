node_modules/
dist/
server/data/
*.db
*.db-wal
*.db-shm
*.bak
# Auto-generated dev encryption key (written next to the SQLite DB when
# ENCRYPTION_KEY is unset outside production). Never commit it.
.encryption-key
.env
.env.local
# Tier-instance secrets (examples are committed; real copies are not)
.env.clean
docker/tiers/clean.config.json
docker/tiers/gray.config.json
.DS_Store

# Personal deployment scripts (contain keys/credentials — kept local)
deploy-pi.sh
update-hermes.sh

# Monetization planning — private, never committed
monetization/
